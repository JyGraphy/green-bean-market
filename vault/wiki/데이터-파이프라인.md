# 데이터 파이프라인

매일 자동 갱신되는 스크래핑 파이프라인의 구조와 건강 상태.

**최종 갱신**: 2026-08-02 · **근거**: [[2026-08-02-스크래퍼점검]]

---

## 현재 상태

| 항목 | 값 |
|------|-----|
| 스크래퍼 | 15개 (파이프라인 연결) |
| 안전장치 규칙 위반 | **0건** |
| 금지 패턴 검출 | **1건** — `scripts/scrape-falcon.js` |

## 실행 순서

`scrapers/run_all.py` → GitHub Actions에서 매일 실행.

1. 스크래퍼 15종 순차 실행 (개별 실패는 격리 — 정상 store는 커밋됨)
2. `scripts/check_links.py` — 404/410만 제거, 403/429는 보존
3. `scripts/enrich_process.py` — '알수없음' 상품의 가공방식 상세페이지 보강
4. `scripts/generate_sql.py` — 파생 파일 재생성
5. `scripts/validate_data.py` — 커밋 직전 검증 게이트
6. `scripts/healthcheck.py` — 배포 후 라이브 사이트 점검 (KST 07:15)

문서(`CLAUDE.md`) 기술과 실제 코드가 **일치**함을 확인했다.

## ⏳ 조치 필요

| 우선순위 | 항목 | 내용 |
|---|------|------|
| 1 | `scripts/scrape-falcon.js` 폐기 | `START_ID + i` 순차 ID 부여 — 규칙 2 금지 패턴. 파이프라인 밖 수동 도구이나 재사용 시 중복 ID 발생. 주석 `START_ID=850`도 낡음(실제 최대 1417) |
| 2 | 모모스·커피리브레 통합 | 자체 저장 로직 사용 → `common.to_products` + `update_json`으로 통합하면 `clean_name` 미적용 문제(`[재입고]` 잔류) 해소 |
| 3 | 아얀투 이름 파싱 | `parse_v2` 폴백이 카드 전체 텍스트를 상품명으로 캡처 → 22건 오염, origin 20/20 미상 |
| 4 | `run_all.py` 타임아웃 | `subprocess.run`에 `timeout=` 없음 → 한 사이트가 멈추면 잡 전체 대기 (블레스빈이 취약) |
| 5 | `abs_url` 통일 | `cobeans:32`, `blessbean:29`가 직접 URL 조립 (현재 결과는 정상) |

## 검수 자동화

```bash
python3 scripts/qa_report.py          # 보고서 2건 생성 — 토큰 0
python3 scripts/qa_report.py --check  # 문제 있으면 exit 1
```

기계적으로 판정 가능한 검사는 전부 이 스크립트가 한다. **AI는 ❌가 나왔을 때만 부른다.**

## 근거 문서

- [[2026-08-02-스크래퍼점검]] — `raw/qa/` · 신뢰도 상 (소스 직접 검사)
- [[2026-08-02-데이터검증]] — `raw/qa/` · 신뢰도 상
